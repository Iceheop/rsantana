---
interface Props {
    src: string;
    description: string;
    title: string;
    tags: string[];
    url: string;
}
const { src, description, title, tags, url } = Astro.props;
---

<article>
    <a href={url} class="text-decoration-none" target="_blank" rel="noopener noreferrer">
        <section class="ratio ratio-16x9 overflow-hidden rounded-3">
            <img src={src} alt={description} class="img-fluid" loading="lazy">
        </section>
        <section class="pt-4">
            <h2 class="fs-5 fw-bold mb-1">{title}</h2>
            <p class="fs-6">{description}</p>
            {tags && tags.length > 0 && (
                <div class="d-flex flex-wrap gap-2 mt-2">
                    {tags.map((t) => (
                        <span class="badge px-3 py-2 rounded-pill text-bg-dark">{t}</span>
                    ))}
                </div>
            )}
        </section>
    </a>
</article>

